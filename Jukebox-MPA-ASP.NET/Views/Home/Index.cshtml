@using Jukebox_MPA_ASP.NET.Models.Database
@using Jukebox_MPA_ASP.NET.Controllers
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
     <form asp-action="UsersDropdown" asp-controller="home" method="post">  
  
        <div class="row">  
                @*<select id="drpEmpList" class="form-control" asp-for="@Users.Name" asp-items="@(new SelectList(Model.Users.Name, "Value", "Text"))">  
                    <option value="">--Select--</option>  
                </select>  *@
@*                <input type="hidden" asp-for="Id" />*@  
                
            <div class="col-md-2">  
                <input type="submit" name="btnSubmit" value="Submit" class="btn btn-success" />  
            </div>  
        </div>  

   </form>  


        @using (Html.BeginForm("OnPost", "HomeController",FormMethod.Post))
    {
        <table cellpadding="0" cellspacing="0">
            <tr>
                <th colspan="2" align="center">New User</th>
                <td>Name</td>
                @*<td>
                    @Html.TextBoxFor(HomeController.inputName)
                </td>*@
            </tr>
        </table>
        }
</div>
